# Fixtures

This directory is reserved for approved public fixture metadata and small reproducible inputs governed by the workflow and provenance specifications. Private holdouts, credentials, and protected access material remain outside the public repository.

Fixtures are versioned and content-addressed; holdout fixtures are never committed.
