# Schemas

This directory contains versioned JSON Schema definitions for workflow specifications and related benchmark records. The canonical Phase-0 workflow schema is `workflow.schema.json`, which follows the specification chain in `WORKFLOW-SPEC.md` and is intended to support independent validation without coupling the benchmark to a particular execution harness.
